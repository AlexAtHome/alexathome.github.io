---
layout: root
eleventyImport:
	collections: ["postlist"]
---

<section id="calling-card" class="py-16 flex relative flex-col justify-center items-start">
	<div class="flex flex-col lg:flex-row-reverse gap-12 items-center container max-w-4xl" role="presentation">
		<img
			src="{% pfp %}"
			alt="github profile"
			class="block object-fill lg:mt-4 self-start w-36 h-36 rounded-full outline-pink outline outline-4 outline-offset-8 mx-auto"
			loading="lazy"
		/>
		<div class="flex flex-col gap-2 text-3xl">
			<h1>
				<div class="text-lg" role="presentation">Hi! Welcome to my corner. I'm</div>
				<strong class="block text-6xl sm:text-7xl font-black leading-none text-transparent bg-clip-text bg-gradient-to-r from-green to-blue">Alex Bolotskov</strong>
			</h1>
			<ul class="flex gap-6 flex-wrap items-center text-sm">
				<li>26 y.o.</li>
				<li>
					{% render 'icon', icon: 'gender-male', className: 'inline-block w-[1em] h-[1em]' %}
					he/him
				</li>
				<li class="inline-flex gap-2" aria-label="Specialty">
					{% assign langs = "angular,react,js,ts,node,html,css" | split: ","  %}
					{% for lang in langs %}
						<svg class="w-6 h-6" width="32" height="32" fill="currentColor" role="presentation" aria-label="{{ lang }}">
							<use xlink:href="/assets/icons/langs.svg#{{- lang -}}" />
						</svg>
					{% endfor %}
				</li>
			</ul>
			<p>A frontend dev, a gamer and&nbsp;a&nbsp;proud Linux user.</p>
			<p>
				I like
				<ul>
					<li>
						{% render 'icon', icon: 'code-slash', className: 'inline-block w-8 h-8' %}
						coding
					</li>
					<li>
						{% render 'icon', icon: 'bicycle', className: 'inline-block w-8 h-8' %}
						cycles
					</li>
					<li>
						{% render 'icon', icon: 'keyboard', className: 'inline-block w-8 h-8' %}
						and good keyboards
					</li>
				</ul>
			</p>
		</div>
	</div>
</section>

<section id="articles" class="py-8 flex relative flex-col justify-center items-start min-h-screen">
	<div class="container" role="presentation">
		<h2 class="mb-20 text-5xl font-bold text-pink">Latest posts</h2>
		<div class="grid grid-cols-1 md:grid-cols-2 gap-8">
			{%- for post in collections.postlist limit:3 -%}
				{%
					render 'article-excerpt',
					href: post.url,
					title: post.data.title,
					excerpt: post.page.excerpt,
					createdAt: post.page.date
				%}
			{%- endfor -%}
			<div class="inline-grid content-center justify-start">
				<a href="/posts/" class="inline-flex gap-2 items-center link group">
					More articles
					{% render 'icon', icon: 'arrow-right', className: 'inline-block w-[1em] h-[1em]' %}
				</a>
			</div>
		</div>
	</div>
</section>

<section id="projects" class="py-8 flex relative flex-col justify-center items-start min-h-screen">
	<div class="container" role="presentation">
		<h2 class="mb-20 text-5xl font-bold text-pink">Projects</h2>
		<div class="grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-8">
			{% render 'project-preview',
				name: "viewport",
				description: "Just a webpage that shows the current viewport and screen resolution",
				sourceCodeUrl: "https://github.com/AlexAtHome/viewport",
				webpageUrl: "https://alexathome.github.io/viewport/",
				icon: 'arrows-fullscreen'
			%}
			{% render 'project-preview',
				name: "json",
				description: "JSON format tool",
				sourceCodeUrl: "https://github.com/AlexAtHome/json",
				webpageUrl: "https://alexathome.github.io/json/",
				icon: 'braces'
			%}
			{% render 'project-preview', 
				name: "vscode-fish-text",
				description: "Soviet style text generator for Visual Studio Code",
				sourceCodeUrl: "https://github.com/AlexAtHome/vscode-fish-text",
				webpageUrl: "https://marketplace.visualstudio.com/items?itemName=mamoru.vscode-fish-text",
				icon: 'body-text'
			%}
			{% render 'project-preview', 
				name: "sitealert",
				description: "A Python script that checks if the given URL does not return the 4xx or 5xx status.",
				sourceCodeUrl: "https://github.com/AlexAtHome/sitealert",
				icon: 'exclamation-diamond'
			%}
			{% render 'project-preview', 
				name: "keycap",
				description: "Keyboard testing app",
				sourceCodeUrl: "https://github.com/AlexAtHome/keycap",
				webpageUrl: "https://alexathome.github.io/keycap/",
				icon: 'keyboard'
			%}
			{% render 'project-preview', 
				name: "minecraft-steve",
				description: "WIP 3D model of Steve from Minecraft&#8482; done entirely (sort of) in CSS"
				sourceCodeUrl: "https://github.com/AlexAtHome/minecraft-steve",
				webpageUrl: "https://alexathome.github.io/minecraft-steve/",
				icon: 'joystick'
			%}
		</div>
	</div>
</section>

<script>
	const frontPageLink = document.querySelector('#front-page-link')
	frontPageLink.addEventListener('click', event => {
		event.preventDefault()
		scrollTo({ top: 0, behavior: 'smooth' })
	})
</script>
